# ✅ COMPLETED: Full Calendar Auto-Adjustment Around Shift Entries

Implemented in `src/lib/shiftScheduleBuilder.ts` and wired into `src/pages/Calendar.tsx`.

All block types (sleep, commute, morning/evening routine, training, gym commutes) now reposition around shift entries.
